# Используйте официальный образ Python 3.12 как базовый
FROM python:3.12-slim

# Установите рабочую директорию в контейнере
WORKDIR /app

# Скопируйте файлы вашего проекта в контейнер
COPY . /app

# Установите необходимые зависимости
# Убедитесь, что все необходимые библиотеки указаны в файле requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Задайте переменную окружения для Python, чтобы вывод не буферизовался
ENV PYTHONUNBUFFERED=1

# Укажите команду для запуска вашего приложения
CMD ["python", "./import_requests.py"]
